import type { EditorConfig } from "@editorjs/editorjs";

// Placeholder Editor.js tools configuration to mirror the target file structure.
export const defaultEditorTools: EditorConfig["tools"] = {};
